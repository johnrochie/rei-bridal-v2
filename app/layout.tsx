import type { Metadata } from "next";
import { Playfair_Display, Inter } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Rei Bridal | Luxury Wedding Dresses & Couture Collections",
  description: "Ireland's premier bridal boutique. Discover our exclusive designer collections, book a private styling appointment, and find your dream wedding dress at Rei Bridal.",
  keywords: "luxury wedding dresses, bridal boutique Ireland, wedding gowns, bridal couture, Rei Bridal",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${playfair.variable} ${inter.variable}`}>
      <body className="antialiased">{children}</body>
    </html>
  );
}
